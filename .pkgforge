name 'libmnl'
org 'amylum'

licenses 'COPYING'

deps(
  'linux-api-headers': '4.10-1'
)

configure_flags(
  prefix: '/usr'
)

cflags
harden

build do
  run './autogen.sh'
  configure
  make
  install
end

test do
  # TODO: add tests
end
